{
  "name": "example",
  "private": true,
  "scripts": {
    "start": "parcel serve index.html",
    "refresh": "yarn cache clean nano-panel && rm -rf $(yarn cache dir)/.tmp && yarn add file:../nano-panel.tgz"
  },
  "dependencies": {
    "nano-panel": "file:../nano-panel.tgz",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-json-view": "1.21.3"
  },
  "devDependencies": {
    "parcel": "2.0.0-beta.2"
  }
}
