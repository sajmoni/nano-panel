{
  "name": "example",
  "private": true,
  "scripts": {
    "start": "parcel serve index.html",
    "refresh": "yarn cache clean nano-panel && rm -rf $(yarn cache dir)/.tmp && yarn install --force --no-lockfile"
  },
  "dependencies": {
    "nano-panel": "file:../nano-panel.tgz",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "parcel": "2.0.0-beta.1"
  }
}
